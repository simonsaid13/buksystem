import React from 'react';
import DesignSystemScreen from '@screens/DesignSystem';

/**
 * App entry point during development.
 * Points to the Design System preview screen.
 * Switch this to a real screen when building the actual app.
 */
export default function Index() {
  return <DesignSystemScreen />;
}
